#title = "Http Toml"

http:
  host: localhost
  port: 8080
  serviceName: demo-service


discover:
  Host: localhost
  port: 8500
  instanceId: user-service

config:
  id: config-server
  profile: "dev"
  label: "master"